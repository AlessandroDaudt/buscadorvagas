[pytest]
addopts = -q
testpaths = tests
markers =
    local_model: requires a locally running Ollama model
    gpu: requires a local NVIDIA GPU available to the runtime
    live_source: accesses an explicitly configured public job source
