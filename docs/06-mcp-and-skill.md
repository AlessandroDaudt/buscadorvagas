# MCP local

Inicie com `docker compose run --rm -i autopilot autopilot mcp` ou `autopilot mcp` em Python local.
As ferramentas `scan_jobs`, `draft_application` e `export_jobs` não exigem chaves. Scan consulta
páginas públicas; draft apenas grava arquivos e export opera totalmente offline.
