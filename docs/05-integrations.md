# Integrações locais

Saídas suportadas: terminal, CSV, JSON, HTML e toast opcional do Windows. MCP, painel, scheduler,
SQLite e Ollama são locais. Não existem notificações ou webhooks externos no fluxo de produção.
