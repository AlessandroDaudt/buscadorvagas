# Checklist de testes

Execute Ruff, mypy, pytest, cobertura e build conforme o README. A suíte normal é offline. Testes
de modelo, GPU e fonte ao vivo usam marcas `local_model`, `gpu` e `live_source` e são opcionais.
