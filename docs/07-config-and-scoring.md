# Configuração e score

Competências e preferências ficam em `config/candidate_profile.json` e
`config/search_preferences.json`, não no scanner. O score determinístico permanece válido sem
Ollama. A revisão local usa schema estrito, não define total e só ajusta componentes dentro do
limite. Consulte [README.md](../README.md).
